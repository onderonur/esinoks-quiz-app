import React from "react";
import { Box, Divider } from "@material-ui/core";

const BaseDivider = () => {
  return (
    <Box marginY={2}>
      <Divider />
    </Box>
  );
};

export default BaseDivider;
